# 3886. Sum of Sortable Integers

记录：独立 · 未标超时 · 首交未记录（迁移自旧 unseen 记录）

## 题意

长度为 n 的数组。若 k 是 n 的因数，且把数组切成长度 k 的连续块、每块独立循环移位后整体可以变成非递减，则 k 为可排序整数。求所有可排序整数之和。

## 我的思考

第一版（逐块扫描判定）已 AC，见 `solutions/3886-first.java`。

之后把每个块的判定优化到 O(1)，即 `solutions/3886.java`。

## 卡点

## 关键点

## 主流解对照

## 复杂度

（补）第一版 O(d(n)·n)；优化版预处理 O(n)，判定总量为 Σ n/k（k 取遍 n 的因数）。

## 注意

## 外部反馈

- AI：每个块的判定可以做到 O(1)，不过面试应该不要求这一步优化。
