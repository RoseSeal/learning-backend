# 3877. Minimum Removals to Achieve Target XOR

记录：H1 · 未标超时 · 首交未记录（迁移自旧 unseen 记录）

> 迁移更正：原记录题号为 3887。按题意（n 至多 40、删除最少元素使剩余异或等于 target）核对为 3877。

## 题意

整数数组，删除最少的元素，使剩余元素的异或结果等于 target；不可能返回 -1。约束：n 可达 40。

## 我的思考

这题又有点没思路。
用bitmask吗？那n=40，总状态数2^40，不可行啊。

提示：遍历顺序可以保证是否选取。

对，我混淆了01背包和bitmask，不需要记下来那个元素具体选没选

## 卡点

- 想用 bitmask，但 n = 40 时 2^40 个状态不可行。

## 关键点

- 混淆了 01 背包和 bitmask：不需要记录具体哪个元素选没选，按遍历顺序做"当前异或值 → 最多保留个数"的背包即可。

## 主流解对照

## 复杂度

（补）O(n · 2^B)，2^B 为不小于最大值的 2 的幂；空间 O(2^B)。

## 注意
