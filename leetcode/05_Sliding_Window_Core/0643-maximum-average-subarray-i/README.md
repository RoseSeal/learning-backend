# 643. Maximum Average Subarray I

## 我的思考
这题是固定长度滑动窗口。

先统计前 `k` 个元素对应的平均值，作为初始窗口。  
之后窗口每次右移一格：
- 加入新的元素贡献
- 移除最左侧元素贡献
- 更新最大值

整体思路很直接，用时 3min10。

## 关键点

## 复杂度
- 时间：`O(n)`
- 空间：`O(1)`

## 注意
1. 可以先维护窗口元素和，最后返回结果时再统一除以 `k`。